<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	xmlns:georss="http://www.georss.org/georss"
        xmlns:geo="http://www.w3.org/2003/01/geo/wgs84_pos#"
        xmlns:media="http://search.yahoo.com/mrss/"><channel>
<title>Lincoln Clarete</title>
<atom:link href="https://clarete.li/rss.xml" rel="self" type="application/rss+xml" />
<link>https://clarete.li/</link>
<description><![CDATA[]]></description>
<language>en</language>
<pubDate>Sun, 31 May 2020 09:50:23 -0400</pubDate>
<lastBuildDate>Sun, 31 May 2020 09:50:23 -0400</lastBuildDate>
<generator>Emacs 28.0 Org-mode 9.3</generator>
<webMaster>lincoln@clarete.li (Lincoln Clarete)</webMaster>
<image>
<url>https://orgmode.org/img/org-mode-unicorn-logo.png</url>
<title>Lincoln Clarete</title>
<link>https://clarete.li/</link>
</image>


<item>
<title>A toy compiler from scratch</title>
<link>https://clarete.li/toy-compiler-from-scratch.html</link>
<author>lincoln@clarete.li (Lincoln Clarete)</author>
<guid isPermaLink="false">https://clarete.li/toy-compiler-from-scratch.html</guid>
<pubDate>Sun, 31 May 2020 00:00:00 -0400</pubDate>

<description><![CDATA[<p>
I've always been fascinated with programming languages.  It's
incredible to me that some programs take the text representation of
our ideas and turn them into code that can be executed by the
computer.
</p>

<p>
Wanting to build my own programming languages, I started to write and
rewrite <a href="https://github.com/clarete/wheelbarrow/blob/master/lispinho/js/main.js">lots</a> <a href="https://github.com/clarete/yal">of</a> <a href="https://gist.github.com/clarete/03e825a70c4b4047468cc9d07ec47e4b">little</a> <a href="https://github.com/clarete/wheelbarrow/blob/master/lispinho/js/main2.js">lisp</a> interpreters. That is a common path for
beginners.  Making these toys gave me better understanding of how
programming languages work, but also made me a better software
engineer in general. So it's definitely worth the exercise :)
</p>

<p>
Today I want to share an exercise of designing <a href="https://github.com/clarete/effigy">Effigy</a>, a little toy
compiler that targets the Python Virtual Machine.  It's far away from
being a production level tool, but it's a bit fancier than the Lisp
interpreters I mentioned above.
</p>

<div class="note">
<p>
<b>Note</b>: Even though Python is said to be a scripting language, it does
have a <a href="https://github.com/python/cpython/blob/3.7/Python/ceval.c">virtual machine</a> that executes the <i>bytecode</i> generated by its
<a href="https://github.com/python/cpython/blob/3.7/Python/compile.c">compiler</a>.  This is where all those <code>.pyc</code> files come from! :)
</p>

</div>

<p>
Effigy takes a syntax that I made up and generates <i>bytecode</i> for that
Virtual Machine.  Without further ado, let's see how it works!
</p>

<p>
A 38k feet view of the process
</p>

<p>
The text representation of the program is parsed into a tree
structure that contains the meaning described in the text program.
That tree is traversed and the <i>bytecode</i> is emitted.
</p>

<div class="centralized">
<p>
<img src="/media/blogimg/effigy-an-experiment-writing-a-compiler-overview.png" alt="Highlevel Overview"/>
</p>

</div>

<p>
Because of its simplicity, Effigy doesn't really apply any
optimization to either the parse tree or to the <i>bytecode</i>.  That's
what keeps Effigy in the toy category.  At least it will make it
easier to understand how it works :)
</p>

<p>
Both the initial phase, that converts text into a parse tree, and
the second phase, that traverse the parse tree and emit code, are
built wit <a href="https://bford.info/pub/lang/peg.pdf">Parsing Expression Grammars</a> (or PEGs for short).  I
learned how to implement the tree scanning used in the second
transformation from the article <a href="http://www.lua.inf.puc-rio.br/publications/mascarenhas11parsing.pdf">Parsing Expression Grammars for
Structured Data</a>.  And during the time that I was implementing
Effigy, I also found the article <a href="http://www.vpri.org/pdf/tr2010003_PEG.pdf">PEG-based transformer provides
front-, middle and back-end stages in a simple compiler</a> that made me
feel more comfortable with my choice and made me tempted to re-write
the assembler as a PEG traversal too, but I never got to it.
</p>

<p>
Although it's not a requirement to understand this post, I highly
recommend reading the articles above as they were essential for me
to really understand how PEGs could be so flexible and powerful.
The PEG library
</p>

<p>
Two out of the three of the main components of Effigy are built on
top of the PEG implementation.  That makes the PEG implementation
itself is a pretty important part of this project, so it's worth
talking about it with some depth before moving on to the actual
compiler.
</p>

<p>
The main goal of the PEG library is to parse input based on a
user-defined grammar and to capture the results when parsing
succeeds.  That happens in roughly three steps:
</p>

<p>
The user-defined grammar is parsed into a grammar tree;
The grammar tree and the input are fed to the grammar interpreter
that traverses the grammar tree and try to match the input.  If
matching is successful, it generates a parse tree with the
captured results;
The parse tree is traversed and semantic actions are applied;
</p>

<p>
The first step is a <a href="https://en.wikipedia.org/wiki/Recursive_descent_parser">recursive descent parser</a> that is able to
understand the syntax of PEG grammars and create a tree that is
traversed by the second step. E.g.: Parsing the grammar following
grammar:
</p>

<div class="org-src-container">
<pre class="src src-peg"><span style="color: #fabd2f;">Number</span> <span style="color: #d3869b;">&lt;-</span> <span style="color: #b8bb26;">[0-9]</span><span style="color: #fe8019;">+</span>
</pre>
</div>

<p>
yields the following grammar tree:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">{</span>
  Number: <span style="color: #b16286;">[</span><span style="color: #8ec07c;">[</span>
    <span style="color: #d65d0e;">[</span><span style="color: #458588;">{</span> name: <span style="color: #b8bb26;">'oneOrMore'</span> <span style="color: #458588;">}</span>,
      <span style="color: #458588;">[</span><span style="color: #b16286;">{</span> name: <span style="color: #b8bb26;">'range'</span> <span style="color: #b16286;">}</span>, <span style="color: #b8bb26;">'0'</span>, <span style="color: #b8bb26;">'9'</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span>
  <span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span>
<span style="color: #458588;">}</span>
</pre>
</div>

<p>
In that format, lists are considered lists unless their first item
is an object.  In that case, they're seen as functions and the rest
of the elements in the list are considered parameters.  That might
resemble Lisp's <a href="https://en.wikipedia.org/wiki/S-expression">S-Expressions</a> for some! This format is the simplest
I found to make the grammar interpreter as simple as possible as
well.
</p>

<p>
In the example above, the grammar interpreter starts from the first
rule of the grammar, <code>Number</code>, that triggers the function
<code>oneOrMore</code> with a <a href="https://en.wikipedia.org/wiki/Thunk">thunk</a> of <code>range</code> with its parameters and execute
it until it fails.
</p>

<p>
The grammar interpreter keeps the cursor of the input as an internal
field that increments when the character under the cursor matches
the piece of the grammar being executed. E.g.: If the above grammar
interpreter receives the above grammar and the input <code>123</code>, when
<code>oneOrMore</code> calls <code>range</code> for the first time, <code>cursor</code> is
incremented from <code>0</code> to <code>1</code>, when <code>oneOrMore</code> calls <code>range</code> again,
<code>cursor</code> is incremented from <code>1</code> to <code>2</code> and so forth.  Notice that
PEGs don't really need to consume the entire input to succeed.  If
you really want a grammar to enforce consuming the whole input to be
deemed successful, you can convey that need with the following
syntax:
</p>

<div class="org-src-container">
<pre class="src src-peg"><span style="color: #fabd2f;">Start</span>  <span style="color: #d3869b;">&lt;-</span> Number <span style="color: #fe8019;">!</span>.
<span style="color: #fabd2f;">Number</span> <span style="color: #d3869b;">&lt;-</span> <span style="color: #b8bb26;">[0-9]</span><span style="color: #fe8019;">+</span>
</pre>
</div>

<p>
The expression <code>!.</code> (or Not Any) only succeeds if <code>EOF</code> has been
reached.
</p>

<p>
The list of all operators available are the ones documented in
Ford's paper with the extension for parsing lists from Medeiros'
paper, and here they are:
</p>

<p>
Matching Functions:
<b>Any()</b> - <code>.</code>: Matches any character but fails on <code>EOF</code>;
<b>Literal(c)</b> - <code>"c"</code>: Fails if <code>c</code> doesn't match the character
under the input cursor;
<b>Class(c[])</b> - <code>[abcd]</code>: Fail if none of the elements of <code>c[]</code>
match the character under the input cursor;
<b>Range(ca, cb)</b> <code>[a-z]</code>: Fail if the current character under the
input cursor isn't between the characteres <code>ca</code> and <code>cb</code>;
</p>

<p>
Parsing Functions:
<b>ZeroOrMore(fn)</b> - Star Operator (*): Execute <code>fn</code> indefinitely
until it fails.  All collected results are returned. It never
fails even if it fails in the first call;
<b>OneOrMore(fn)</b> - Plus Operator (+): Execute <code>fn</code> once failing if
this first call fails. If the first call succeeds, then prepend
this result to the output of <code>ZeroOrMore(fn)</code>
<b>Option(fn)</b> - Option Operator (?): Return the result of <code>fn()</code> or
<code>null</code> if the call to <code>fn</code> fails.
<b>Choice(fn[])</b> - Ordered Choice Operator (/): Iterate over <code>fn[]</code>,
and return the result of the first function that succeeds. It can
be seen as an OR operation.
</p>

<p>
Syntactic Predicate Functions:
<b>Not(fn)</b> - <code>!</code>: Return true if <code>fn</code> fails and false if <code>fn</code>
succeeds;
<b>And(fn)</b> - <code>&amp;</code>: The opposite of <b>Not</b> or <code>Not(Not(fn))</code>;
</p>

<p>
List Parsing Function:
</p>

<p>
<b>Open()</b> - <code>{</code>: Return true if the element under the cursor is a
list;
<b>Close()</b> - <code>}</code>: Return true if the currently open list has ended
</p>

<div id="outline-container-org9460a14" class="outline-3">
<h3 id="org9460a14">Scanner Interface</h3>
<div class="outline-text-3" id="text-org9460a14">
<p>
The parser that implements the PEG interface listed above is built
on top of a scanner that provides all the matching functions and
the ones that backtracks the input cursor.
</p>

<p>
This is the interface that the matching functions depend:
<b>Scanner(input)</b>: Constructor that creates a new instance of the
scanner taking the input as a parameter;
<b>Current()</b>: Return what's under the scanner's cursor;
<b>EOS()</b>: Determine if the current element is the end of the input;
<b>Error()</b>: Generate a parsing error;
<b>Expect(e)</b>: Return the current element under the cursor if it
matches <code>e</code> or throw an error otherwise. Doesn't move input
cursor;
<b>Match(e)</b>: Return the current element under the cursor if it
matches <code>e</code> and advance the cursor by the size of <code>e</code>;
<b>Next()</b>: Advance the input cursor;
</p>

<p>
The parsing function <code>Choice</code> is also implemented in the scanner
because it needs direct control over the input cursor in order to
backtrack before a new option is attempted. E.g.:
</p>

<div class="org-src-container">
<pre class="src src-js"><span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Part of the JavaScript implementation of the scanner</span>
<span style="color: #fb4933;">function</span> <span style="color: #fabd2f;">choice</span><span style="color: #458588;">(</span>...fns<span style="color: #458588;">)</span> <span style="color: #458588;">{</span>
  <span style="color: #fb4933;">const</span> <span style="color: #83a598;">saved</span> = cursor; <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">input cursor</span>
  <span style="color: #fb4933;">for</span> <span style="color: #b16286;">(</span><span style="color: #fb4933;">const</span> <span style="color: #83a598;">fn</span> of fns<span style="color: #b16286;">)</span> <span style="color: #b16286;">{</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Once an alternative succeeds, all the other ones</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">are discarded.</span>
    <span style="color: #fb4933;">try</span> <span style="color: #8ec07c;">{</span> <span style="color: #fb4933;">return</span> fn<span style="color: #d65d0e;">()</span>; <span style="color: #8ec07c;">}</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">If an alternative fails, the cursor is backtracked</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">to where it was in the beginning of the choice</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">operation.</span>
    <span style="color: #fb4933;">catch</span> <span style="color: #8ec07c;">(</span>e<span style="color: #8ec07c;">)</span> <span style="color: #8ec07c;">{</span> cursor = saved; <span style="color: #8ec07c;">}</span>
  <span style="color: #b16286;">}</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">If no alternatives succeed, the choice operation</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">fails.</span>
  <span style="color: #fb4933;">throw</span> <span style="color: #fb4933;">new</span> <span style="color: #d3869b;">Error</span><span style="color: #b16286;">(</span><span style="color: #b8bb26;">"None of the options matched"</span><span style="color: #b16286;">)</span>;
<span style="color: #458588;">}</span>
</pre>
</div>

<p>
The syntactic predicate <code>Not</code> is implemented in the scanner as well
since it backtracks the input cursor after being executed in order
to provide infinite look-ahead.
</p>

<p>
The recursive descent parser generating grammar trees off PEG
grammars is built on top of the scanner interface and the PEG
functions (ZeroOrMore, Option, Choice, etc).  The separation of the
scanner interface from the implementation of the PEG functions
allowed the construction of the two different scanners: one for
text and another one for other data structures (lists).
</p>

<p>
To make things a bit less abstract, here's an example of each
scanner in action:
</p>
</div>

<div id="outline-container-org656fbc0" class="outline-4">
<h4 id="org656fbc0">Text Scanner</h4>
<div class="outline-text-4" id="text-org656fbc0">
<p>
The <code>match</code> function takes a string as its input:
</p>

<div class="org-src-container">
<pre class="src src-js">peg.pegc<span style="color: #458588;">(</span><span style="color: #b8bb26;">`Number &lt;- [0-9]+`</span><span style="color: #458588;">)</span>.match<span style="color: #458588;">(</span><span style="color: #b8bb26;">'1234'</span><span style="color: #458588;">)</span>
</pre>
</div>

<p>
which generates:
</p>
<div class="org-src-container">
<pre class="src src-js"><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Number'</span>, <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'1'</span>, <span style="color: #b8bb26;">'2'</span>, <span style="color: #b8bb26;">'3'</span>, <span style="color: #b8bb26;">'4'</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>
</pre>
</div>
</div>
</div>

<div id="outline-container-org1f094f7" class="outline-4">
<h4 id="org1f094f7">List Scanner</h4>
<div class="outline-text-4" id="text-org1f094f7">
<p>
The <code>matchl</code> function takes a list as its input:
</p>

<div class="org-src-container">
<pre class="src src-js">peg.pegc<span style="color: #458588;">(</span><span style="color: #b8bb26;">`</span>
<span style="color: #b8bb26;">  Numbers &lt;- { "Number" { Atom+ } }</span>
<span style="color: #b8bb26;">  Atom    &lt;- !{ .* } .</span>
<span style="color: #b8bb26;">`</span><span style="color: #458588;">)</span>.matchl<span style="color: #458588;">(</span><span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Number'</span>, <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'1'</span>, <span style="color: #b8bb26;">'2'</span>, <span style="color: #b8bb26;">'3'</span>, <span style="color: #b8bb26;">'4'</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">)</span>
</pre>
</div>

<p>
which generates:
</p>
<div class="org-src-container">
<pre class="src src-js"><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Numbers'</span>,
  <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Number'</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Atom'</span>, <span style="color: #b8bb26;">'1'</span><span style="color: #d65d0e;">]</span>,
     <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Atom'</span>, <span style="color: #b8bb26;">'2'</span><span style="color: #d65d0e;">]</span>,
     <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Atom'</span>, <span style="color: #b8bb26;">'3'</span><span style="color: #d65d0e;">]</span>,
     <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Atom'</span>, <span style="color: #b8bb26;">'4'</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>
</pre>
</div>
</div>
</div>
</div>

<div id="outline-container-org41300cd" class="outline-3">
<h3 id="org41300cd">Semantic Actions</h3>
<div class="outline-text-3" id="text-org41300cd">
<p>
After collecting the results from the matching operations and
nesting them following the grammar's structure, the PEG library can
also apply custom functions on the results of each rule
execution. E.g.:
</p>

<div class="org-src-container">
<pre class="src src-js"><span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">JavaScript API for compiling a grammar and binding</span>
<span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">semantic actions to the generated parser</span>
<span style="color: #fb4933;">const</span> <span style="color: #83a598;">grammar</span> = <span style="color: #b8bb26;">`</span>
<span style="color: #b8bb26;">  Additive  &lt;- Multitive '+' Additive / Multitive</span>
<span style="color: #b8bb26;">  Multitive &lt;- Primary '*' Multitive / Primary</span>
<span style="color: #b8bb26;">  Primary   &lt;- '(' Additive ')' / Number</span>
<span style="color: #b8bb26;">  Number    &lt;- [0-9]+</span>
<span style="color: #b8bb26;">`</span>;
<span style="color: #fb4933;">const</span> <span style="color: #83a598;">parser</span> = peg.pegc<span style="color: #458588;">(</span>grammar<span style="color: #458588;">)</span>.bind<span style="color: #458588;">(</span><span style="color: #b16286;">{</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">The name of the action must correspond to the name</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">of the rule present in the grammar string.  The</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">function `visit` gives the control over when the</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">child nodes will be visited.  That allows executing</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">code before and after visiting happens.  This is</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">very useful during the code generation step on</span>
  <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">the compiler.</span>
  Additive: <span style="color: #8ec07c;">(</span><span style="color: #d65d0e;">{</span> visit <span style="color: #d65d0e;">}</span><span style="color: #8ec07c;">)</span> =&gt; <span style="color: #8ec07c;">{</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">v</span> = visit<span style="color: #d65d0e;">()</span>;
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Ignore the string '+'</span>
    <span style="color: #fb4933;">return</span> Array.isArray<span style="color: #d65d0e;">(</span>v<span style="color: #d65d0e;">)</span> ? v<span style="color: #d65d0e;">[</span>0<span style="color: #d65d0e;">]</span> + v<span style="color: #d65d0e;">[</span>2<span style="color: #d65d0e;">]</span> : v;
  <span style="color: #8ec07c;">}</span>,
  Multitive: <span style="color: #8ec07c;">(</span><span style="color: #d65d0e;">{</span> visit <span style="color: #d65d0e;">}</span><span style="color: #8ec07c;">)</span> =&gt; <span style="color: #8ec07c;">{</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">v</span> = visit<span style="color: #d65d0e;">()</span>;
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Ignore the string '*'</span>
    <span style="color: #fb4933;">return</span> Array.isArray<span style="color: #d65d0e;">(</span>v<span style="color: #d65d0e;">)</span> ? v<span style="color: #d65d0e;">[</span>0<span style="color: #d65d0e;">]</span> * v<span style="color: #d65d0e;">[</span>2<span style="color: #d65d0e;">]</span> : v;
  <span style="color: #8ec07c;">}</span>,
  Primary: <span style="color: #8ec07c;">(</span><span style="color: #d65d0e;">{</span> visit <span style="color: #d65d0e;">}</span><span style="color: #8ec07c;">)</span> =&gt; <span style="color: #8ec07c;">{</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">v</span> = visit<span style="color: #d65d0e;">()</span>;
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Remove '(' and ')'</span>
    <span style="color: #fb4933;">return</span> Array.isArray<span style="color: #d65d0e;">(</span>v<span style="color: #d65d0e;">)</span> ? v<span style="color: #d65d0e;">[</span>1<span style="color: #d65d0e;">]</span> : v;
  <span style="color: #8ec07c;">}</span>,
  Number: <span style="color: #8ec07c;">(</span><span style="color: #d65d0e;">{</span> visit <span style="color: #d65d0e;">}</span><span style="color: #8ec07c;">)</span> =&gt; <span style="color: #8ec07c;">{</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">v</span> = visit<span style="color: #d65d0e;">()</span>;
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">n</span> = Array.isArray<span style="color: #d65d0e;">(</span>v<span style="color: #d65d0e;">)</span> ? v.join<span style="color: #d65d0e;">(</span><span style="color: #b8bb26;">''</span><span style="color: #d65d0e;">)</span> : v;
    <span style="color: #fb4933;">return</span> parseInt<span style="color: #d65d0e;">(</span>n, 10<span style="color: #d65d0e;">)</span>;
  <span style="color: #8ec07c;">}</span>
<span style="color: #b16286;">}</span><span style="color: #458588;">)</span>;

assertTrue<span style="color: #458588;">(</span>parser<span style="color: #b16286;">(</span><span style="color: #b8bb26;">'42'</span><span style="color: #b16286;">)</span> === 42<span style="color: #458588;">)</span>;
assertTrue<span style="color: #458588;">(</span>parser<span style="color: #b16286;">(</span><span style="color: #b8bb26;">'40+2*5'</span><span style="color: #b16286;">)</span> === 50<span style="color: #458588;">)</span>;
assertTrue<span style="color: #458588;">(</span>parser<span style="color: #b16286;">(</span><span style="color: #b8bb26;">'(40+2)*5'</span><span style="color: #b16286;">)</span> === 210<span style="color: #458588;">)</span>;
</pre>
</div>

<p>
One of the effects of the infinite look-ahead, and the backtracking
specifically, is that the entire input has to be consumed before
deciding if the results are correct or not.  In other words, the
semantic action application happens as an entirely different
traversal <b>after</b> matching is successful.
</p>

<p>
This is explored in depth in the article <a href="https://ohmlang.github.io/pubs/dls2016/modular-semantic-actions.pdf">Modular Semantic Actions</a>
and the general suggestion this implementation follows is that the
semantic action application only happens after parsing finishes
successfully.
</p>

<p>
The Compiler
</p>

<p>
Now that we covered the PEG implementation, we're ready to tackle
the compilation process itself!
</p>
</div>
</div>

<div id="outline-container-orgb457f49" class="outline-3">
<h3 id="orgb457f49">Parsing the program text</h3>
<div class="outline-text-3" id="text-orgb457f49">
<p>
The first stage of the compiler <a href="https://github.com/clarete/effigy/blob/master/lang.peg">is a PEG grammar</a> that scan and
parse the program text and generate an <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree">Abstract Syntax Tree</a> (or AST
for short) off the syntax I made up.  The semantic actions
associated with that grammar join lists of characters into words,
convert lists of digits into numbers, tweak the shape of the AST to
make it less verbose and easier to be traversed and lastly help
overcoming two shortcomings of the PEG implementation:
</p>

<p>
Handle left recursion
Decide if a result should be wrapped into the name of its
parsing rule
</p>

<p>
There are a few documented ways to handling left recursion on
PEGs. The nicest one I found is via <b><b>Bounded Left Recursion</b></b>.
That approach is described in depth in the article <a href="http://www.inf.puc-rio.br/~roberto/docs/sblp2012.pdf">Left Recursion
in Parsing Expression Grammars</a>, but I didn't get to fully implement
it, so I put it aside to focus on getting to a working compiler.
</p>

<p>
The second problem of wrapping captured values with the rule name
or not could have been fixed by adding a new operator to the PEG
implementation and resolved at the grammar level.  But instead I
chose to implement that using semantic actions since the code
needed was simple although a bit verbose.  But everything else
worked out pretty smoothly.  That's enough of background, let's
look at an example. The following code:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #fb4933;">fn</span> <span style="color: #fabd2f;">sum</span><span style="color: #458588;">(</span>a, b<span style="color: #458588;">)</span> a + b
print<span style="color: #458588;">(</span>sum<span style="color: #b16286;">(</span>2, 3<span style="color: #b16286;">)</span><span style="color: #458588;">)</span>
</pre>
</div>

<p>
should generate the following AST:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Module'</span>,
  <span style="color: #b16286;">[</span><span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Statement'</span>,
    <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Function'</span>,
     <span style="color: #458588;">[</span><span style="color: #b8bb26;">'sum'</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Params'</span>, <span style="color: #8ec07c;">[</span><span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Param'</span>, <span style="color: #b8bb26;">'a'</span><span style="color: #d65d0e;">]</span>, <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Param'</span>, <span style="color: #b8bb26;">'b'</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Code'</span>,
       <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Statement'</span>,
        <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'BinOp'</span>, <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'a'</span><span style="color: #458588;">]</span>, <span style="color: #b8bb26;">'+'</span>, <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'b'</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span>,
   <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Statement'</span>,
    <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Call'</span>,
     <span style="color: #458588;">[</span><span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'print'</span><span style="color: #b16286;">]</span>,
      <span style="color: #b16286;">[</span><span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Call'</span>,
        <span style="color: #d65d0e;">[</span><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'sum'</span> <span style="color: #458588;">]</span>,
         <span style="color: #458588;">[</span><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Value'</span>, <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Number'</span>, 2<span style="color: #b16286;">]</span><span style="color: #458588;">]</span>,
          <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Value'</span>, <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Number'</span>, 3<span style="color: #b16286;">]</span><span style="color: #458588;">]</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>
</pre>
</div>

<p>
Notice that <code>fn sum(a, b) { return a + b }</code> outputs the same tree
as <code>fn sum(a, b) a + b</code>.  Code blocks accept either a single
statement or a list of statements within curly brackets (<code>{}</code>).
</p>
</div>
</div>

<div id="outline-container-org3339c03" class="outline-3">
<h3 id="org3339c03">Mapping out scope rules</h3>
<div class="outline-text-3" id="text-org3339c03">
<p>
After generating the AST during the text parsing phase, we need to
go through an additional step before translating the AST into
<i>bytecode</i>.  The scope of every variable needs to be mapped into
one of the three categories:
</p>

<p>
Local variables
Global variables
Free variables
</p>

<p>
Let's look at the following code snippet to talk about it:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #fb4933;">fn</span> <span style="color: #fabd2f;">plus_n</span><span style="color: #458588;">(</span>x<span style="color: #458588;">)</span> <span style="color: #fb4933;">fn</span><span style="color: #458588;">(</span>y<span style="color: #458588;">)</span> x + y
plus_five = plus_n<span style="color: #458588;">(</span>5<span style="color: #458588;">)</span>
print<span style="color: #458588;">(</span>plus_five<span style="color: #b16286;">(</span>2<span style="color: #b16286;">)</span><span style="color: #458588;">)</span> <span style="color: #7c6f64;"># Equals 7</span>
</pre>
</div>

<p>
In the example above, <code>x</code> is declared at the scope created by the
<code>plus_n</code> function and must be available when it's summed to <code>y</code>
within the scope of the anonymous function.  The variable <code>y</code> is a
local variable since it gets created and destroyed within the same
scope, but <code>x</code> is a free variable.
</p>

<p>
Free variables are variables available in the lexical scope that
must be kept around to be used when the scope that declared these
variables isn't around anymore.
</p>

<p>
Global variables seem to exist in Python for performance reasons.
The Python interpreter skips look ups on the local scope for names
that are known to be available in the module scope or within the
built-in module, like the name <code>print</code> in the example above.
</p>

<p>
The process of mapping variables into the aforementioned categories
is done by traversing the AST using a <a href="https://github.com/clarete/effigy/blob/master/lang.tr">second PEG grammar</a> for
parsing lists instead of a stream of characters.  During that
process, a symbol table is built and the AST is annotated with
information that allows the translation phase to look up each
variable in the symbol table.
</p>

<p>
The following Effigy snippet
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #fb4933;">fn</span> <span style="color: #fabd2f;">plus_n</span><span style="color: #458588;">(</span>x<span style="color: #458588;">)</span> <span style="color: #fb4933;">fn</span> <span style="color: #458588;">(</span>y<span style="color: #458588;">)</span> x + y
</pre>
</div>

<p>
generates an annotated AST that looks like this:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Module'</span>,
  <span style="color: #b16286;">[</span><span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Statement'</span>,
    <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Function'</span>,
     <span style="color: #458588;">[</span><span style="color: #b16286;">[</span><span style="color: #b8bb26;">'ScopeId'</span>, 2<span style="color: #b16286;">]</span>, <span style="color: #b8bb26;">'plus_n'</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Params'</span>, <span style="color: #8ec07c;">[</span><span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Param'</span>, <span style="color: #b8bb26;">'x'</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Code'</span>,
       <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Statement'</span>,
        <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Lambda'</span>,
         <span style="color: #458588;">[</span><span style="color: #458588;">[</span><span style="color: #b8bb26;">'ScopeId'</span>, 1<span style="color: #458588;">]</span>,
          <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Params'</span>, <span style="color: #b16286;">[</span><span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Param'</span>, <span style="color: #b8bb26;">'y'</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>,
          <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Code'</span>,
           <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Statement'</span>,
            <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'BinOp'</span>,
             <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'x'</span><span style="color: #d65d0e;">]</span>, <span style="color: #b8bb26;">'+'</span>, <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Load'</span>, <span style="color: #b8bb26;">'y'</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>
</pre>
</div>

<p>
The <code>ScopeId</code> nodes introduced within each scope are used during
the compilation process to look up the nth entry within the current
scope of the symbol table.  Here's a simplified view of the list of
fields a symbol table for the above snippet contains:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">[</span><span style="color: #b16286;">{</span>
  node: <span style="color: #b8bb26;">'module'</span>,
  fast: <span style="color: #8ec07c;">[]</span>,
  deref: <span style="color: #8ec07c;">[]</span>,
  globals: <span style="color: #8ec07c;">[]</span>,
  children: <span style="color: #8ec07c;">[</span><span style="color: #d65d0e;">{</span>
    node: <span style="color: #b8bb26;">'function'</span>,
    fast: <span style="color: #458588;">[]</span>,
    deref: <span style="color: #458588;">[</span><span style="color: #b8bb26;">'x'</span><span style="color: #458588;">]</span>,
    globals: <span style="color: #458588;">[]</span>,
    children: <span style="color: #458588;">[</span><span style="color: #b16286;">{</span>
      node: <span style="color: #b8bb26;">'lambda'</span>,
      fast: <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'y'</span><span style="color: #8ec07c;">]</span>,
      deref: <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'x'</span><span style="color: #8ec07c;">]</span>,
      globals: <span style="color: #8ec07c;">[]</span>,
      children: <span style="color: #8ec07c;">[]</span>
    <span style="color: #b16286;">}</span><span style="color: #458588;">]</span>
  <span style="color: #d65d0e;">}</span><span style="color: #8ec07c;">]</span>
<span style="color: #b16286;">}</span><span style="color: #458588;">]</span>
</pre>
</div>

<p>
One last thing that might be interesting to mention about scopes is
that Python tries to figure out if a variable is a free variable by
comparing where it was assigned and where it was used.  If it is
assigned in the same scope that it's being used, it is a local
variable.  If it is assigned in an enclosing scope, it is a free
variable.  If one needs to reassign a free variable in an inner
scope, the <a href="https://www.python.org/dev/peps/pep-3104/">nonlocal</a> keyword is required to inform the Python
compiler that the assignment isn't local.
</p>

<p>
I chose a slightly different way to allow reassigning free
variables from enclosing scopes.  Effigy provides the <code>let</code> keyword
to mark variables as free variables at the outer scope:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #fb4933;">fn</span> <span style="color: #fabd2f;">f</span><span style="color: #458588;">(</span>input<span style="color: #458588;">)</span> <span style="color: #458588;">{</span>
  <span style="color: #fb4933;">let</span> <span style="color: #83a598;">c</span> = 0
  <span style="color: #fb4933;">fn</span> <span style="color: #fabd2f;">next</span><span style="color: #b16286;">()</span> <span style="color: #b16286;">{</span>
    <span style="color: #83a598;">value</span> = input<span style="color: #8ec07c;">[</span>c<span style="color: #8ec07c;">]</span>
    <span style="color: #83a598;">c</span> = c + 1
    <span style="color: #fb4933;">return</span> value
  <span style="color: #b16286;">}</span>
  <span style="color: #fb4933;">return</span> next
<span style="color: #458588;">}</span>
cursor = f<span style="color: #458588;">(</span><span style="color: #b8bb26;">"word"</span><span style="color: #458588;">)</span>
print<span style="color: #458588;">(</span>cursor<span style="color: #b16286;">()</span><span style="color: #458588;">)</span> <span style="color: #7c6f64;"># prints "w"</span>
print<span style="color: #458588;">(</span>cursor<span style="color: #b16286;">()</span><span style="color: #458588;">)</span> <span style="color: #7c6f64;"># prints "o"</span>
</pre>
</div>

<p>
I haven't used Effigy enough to know if that was a good choice
though :)
</p>

<p>
I bet there might be a way of bundling the symbol table and
generating the code in a single pass, but that wasn't the route I
took.  Quite a few decisions I made for handling variable scope
were inspired by the beautiful write up <a href="https://codewords.recurse.com/issues/seven/dragon-taming-with-tailbiter-a-bytecode-compiler">Dragon taming with
Tailbiter, a bytecode compiler for Python</a> and that's the route that
Darius Bacon took on his experiment.  I highly recommend reading
that post.  It's enlightening and might help understanding the rest
of this post since I won't get into too many details about how
Python itself woks.
</p>
</div>
</div>

<div id="outline-container-org44c4808" class="outline-3">
<h3 id="org44c4808">Output format</h3>
<div class="outline-text-3" id="text-org44c4808">
<p>
Once the AST is annotated by the scope traversal step, it is ready
to be fed once again into the <a href="https://github.com/clarete/effigy/blob/master/lang.tr">second PEG grammar</a> to be traversed
once more, but now with the intent of driving the assembler to
generate code.  In this step, the functions (and modules) in Effigy
are assembled into <a href="https://docs.python.org/3.7/library/dis.html#python-bytecode-instructions"><i>bytecode</i> instructions</a> and bundled into <a href="https://docs.python.org/3/c-api/code.html">Code
objects</a>.
</p>

<p>
Instances of Code objects store <i>bytecode</i> within the <code>co_code</code>
attribute.  They also store metadata, like the number of arguments
a function receives (<code>co_argcount</code>) or the number of local
variables (<code>co_nlocals</code>) for example.  The other very important
data Code objects store is tables with values.  There's one table
for literal values (<code>co_consts</code>), one for local variables
(<code>co_varnames</code>), one for free variables (<code>co_freevars</code>) and one for
global variables (<code>co_names</code>).
</p>

<p>
All these tables are indexed with integers and carry <code>PyObject</code>
instances within them.  And since functions themselves are
<code>PyObject</code> instances, Code object is a recursive data type.
</p>

<p>
When the compiler enters a new scope, a Code object instance is
created, <i>bytecode</i> is generated and tables are filled with data.
When the compiler leaves a scope, the Code object instance is
returned and bundled within the outer Code object, up until the
module scope, which is the top one.
</p>

<p>
When code generation is done, the Code object is written into a
buffer and a header with the following four 32 bit fields is built:
</p>

<p>
magic number
<a href="https://www.python.org/dev/peps/pep-0552/">PEP-552</a> field (Allows deterministic builds of <code>.pyc</code> files)
Modified Date
Size of the code buffer
</p>

<p>
The last step is to write the header and the code buffer into a
<code>.pyc</code> file.
</p>
</div>
</div>

<div id="outline-container-org74365b0" class="outline-3">
<h3 id="org74365b0">Code Generation</h3>
<div class="outline-text-3" id="text-org74365b0">
<p>
Generating the assembly code for filling in the <code>co_code</code>
attributes of Code objects is certainly the biggest task performed
by the compiler.  Let's take a look at how the compiler would
generate code for the following expression <code>result = 2 + 3 * 4</code>.
</p>

<p>
First the following AST is generated:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">[</span><span style="color: #b8bb26;">'Module'</span>,
  <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Statement'</span>,
   <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Assignment'</span>,
    <span style="color: #d65d0e;">[</span><span style="color: #458588;">[</span><span style="color: #b8bb26;">'BinOp'</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'Value'</span>, <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Number'</span>, 2<span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span>,
      <span style="color: #b8bb26;">'+'</span>,
      <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'BinOp'</span>,
       <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Value'</span>, <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Number'</span>, 3<span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span>,
       <span style="color: #b8bb26;">'*'</span>,
       <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'Value'</span>, <span style="color: #d65d0e;">[</span><span style="color: #b8bb26;">'Number'</span>, 4<span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>,
     <span style="color: #458588;">[</span><span style="color: #b8bb26;">'Store'</span>, <span style="color: #b8bb26;">'result'</span><span style="color: #458588;">]</span><span style="color: #d65d0e;">]</span><span style="color: #8ec07c;">]</span><span style="color: #b16286;">]</span><span style="color: #458588;">]</span>
</pre>
</div>

<p>
When the translation phase takes the above tree as input, it
outputs the following Code object:
</p>

<div class="org-src-container">
<pre class="src src-effigy"><span style="color: #458588;">{</span>
  constants: <span style="color: #b16286;">[</span>2, 3, 4, <span style="color: #d3869b;">null</span><span style="color: #b16286;">]</span>,
  names: <span style="color: #b16286;">[</span><span style="color: #b8bb26;">'result'</span><span style="color: #b16286;">]</span>,
  instructions: <span style="color: #b16286;">[</span>
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'load-const'</span>, 0<span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'load-const'</span>, 1<span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'load-const'</span>, 2<span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'binary-multiply'</span><span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'binary-add'</span><span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'store-name'</span>, 0<span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'load-const'</span>, 3<span style="color: #8ec07c;">]</span>,
    <span style="color: #8ec07c;">[</span><span style="color: #b8bb26;">'return-value'</span><span style="color: #8ec07c;">]</span>
  <span style="color: #b16286;">]</span>,
<span style="color: #458588;">}</span>
</pre>
</div>

<p>
Notice that the <code>load-const</code> instructions have an index of the
<code>constants</code> table as its parameter.  That's how the Python Virtual
Machine figures out which constant is being referred and what value
to push to the stack.  The <code>binary-multiply</code> and <code>binary-add</code>
instructions pop data from the stack, perform their respective
operations, and then push the result back to the stack.  The
instruction <code>store-name</code> pops the value left by <code>binary-add</code> off
the stack and save it into the variable referenced in the <code>names</code>
table.  The last <code>load-const</code> is there because all Code objects
must return.  And when a return statement isn't explicitly provided
(like in module scopes), <code>null</code> (which represents Python's <code>None</code>)
is returned.
</p>

<div class="note">
<p>
If you want to check out all the other cases that effigy can
handle, <a href="https://github.com/clarete/effigy/blob/master/spec/lang.spec.js">look at the specs</a> I wrote while I was putting the compiler
together.
</p>

</div>

<p>
It's interesting to mention that the semantic actions for AST nodes
that interact with the values tables in the Code object have two
jobs.  They have to either save or load values from the tables and
also emit instructions.  The semantic action for the <code>Number</code> nodes
is a good example to shown how it's done:
</p>

<div class="org-src-container">
<pre class="src src-js"><span style="color: #458588;">{</span>
  Number: <span style="color: #b16286;">(</span><span style="color: #8ec07c;">{</span> visit <span style="color: #8ec07c;">}</span><span style="color: #b16286;">)</span> =&gt; <span style="color: #b16286;">{</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Visit the actual value to join the digits and</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">convert to a JavaScript integer</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">value</span> = visit<span style="color: #8ec07c;">()[</span>1<span style="color: #8ec07c;">]</span>;
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Push the value to the constants table if it</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">isn't there yet</span>
    <span style="color: #fb4933;">const</span> <span style="color: #83a598;">index</span> = addToTable<span style="color: #8ec07c;">(</span>attr<span style="color: #d65d0e;">(</span><span style="color: #b8bb26;">'constants'</span><span style="color: #d65d0e;">)</span>, value<span style="color: #8ec07c;">)</span>;
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">Emit the instruction with the index of the</span>
    <span style="color: #7c6f64;">// </span><span style="color: #7c6f64;">constant as the parameter to the instruction</span>
    emit<span style="color: #8ec07c;">(</span><span style="color: #b8bb26;">'load-const'</span>, index<span style="color: #8ec07c;">)</span>;
  <span style="color: #b16286;">}</span>
<span style="color: #458588;">}</span>
</pre>
</div>
</div>
</div>

<div id="outline-container-orgbc0f45b" class="outline-3">
<h3 id="orgbc0f45b">Assemble binary data</h3>
<div class="outline-text-3" id="text-orgbc0f45b">
<p>
Besides emitting instructions, the assembler has to support
back-patching of values that were not known ahead of time.  Three
little functions on the assembler's interface allowed that to
happen:
</p>

<p>
<code>pos()</code>: Return the index of the current instruction;
<code>ref()</code>: Push the index of the current instruction to a list of
labels and return the index of the label;
<code>fix(label, value)</code>: Replace the instruction within <code>label</code> with
<code>value</code>.
</p>

<p>
For the sake of completion, here are the other functions that
comprise the assembler's interface (already mentioned on previous
sections):
</p>

<p>
<code>enter()</code>: Enter a new scope, creating a new Code object
instance;
<code>leave()</code>: Return the Code object built for the current scope and
set the enclosing scope as the current one;
<code>emit(opcode, argument)</code>: Append
<code>attr(name, value=undefined)</code>: Helper function for reading or
writing a value to one of the values tables of the current scope;
</p>

<p>
To not have to deal with binary code the whole time I worked on
this toy, I wrote a dummy implementation used for debug purposes
that contained actual JavaScript lists, strings and numbers.
</p>

<p>
After validating that the code generation produced what I intended,
I just swapped assemblers and used the one that actually knew how
to marshal JavaScript objects into the format that the Python
Virtual Machine could understand.
</p>

<p>
The part of the assembler that marshals JavaScript objects into the
binary format that the Python Virtual Machine can read is mostly a
translation of the code under <a href="https://github.com/python/cpython/blob/3.7/Python/marshal.c">Python/marshal.c</a> to JavaScript.
</p>

<p>
Final Thoughts
</p>

<p>
As you must have noticed, there's way more information about the
architecture of the compiler and the PEG implementation than about
how Python works.  That's sort of intentional, because my first goal
with this little experiment was to learn more about compilers and
since Python is the language I know the most, I used knowledge I
already had to put it together.  That being said, I still learned a
whole lot about Python itself on this short journey.  Python is
indeed a fascinating language and its simplicity matches the beauty
of a poem.
</p>

<p>
Although the compiler actually produces files that can be executed
by Python, there's a whole lot missing that I didn't even intend to
cover. Here are a few:
</p>

<p>
The Effigy compiler has the worst error reporting you can
possibly imagine.  If an input has a syntax error, it will only
give you the index of the character that it can't recognize.  If
the error is on the translation after the syntax tree has been
built, you're going to have to print out the AST and figure it
out yourself.
The Effigy language doesn't support lots of very useful features
provided by Python and required for a minimally comfortable
programming experience, like declaring classes, importing other
modules, creating dictionaries with literals (you can still call
the <code>dict()</code> function though :D).
As I mentioned before, the code generated isn't optimized in any
way, so it will most likely be way slower than code generated by
the Python compiler. It's still something I want to do in another
exercise, but I bet Effigy won't ever see that improvement.
</p>

<p>
Although this was just a simple exercise completely focused in
learning about more about programming languages and having fun, I am
curious to hear what you think. If this article or Effigy sparkled
your curiosity, don't hesitate in reaching out!  Send me an email or
find me on the <a href="https://freenode.net/">freenode</a> IRC network (I'm usually hanging out on the
<code>#emacs</code> channel).
</p>
</div>
</div>
]]></description>
</item>
<item>
<title>Starting a blog</title>
<link>https://clarete.li/starting-a-blog.html</link>
<author>lincoln@clarete.li (Lincoln Clarete)</author>
<guid isPermaLink="false">https://clarete.li/starting-a-blog.html</guid>
<pubDate>Sun, 26 Apr 2020 00:00:00 -0400</pubDate>

<description><![CDATA[<p>
I've tried blogging before. All the attempts ended up following a very
similar pattern: Took me a veeeery long time to write a single post
and after that I never looked back at it again.
</p>

<p>
This time I'm trying things differently. I'm not going to start from
the perfect blog post. I'll start just documenting how I turned this
GitHub Pages repository in a blog and see what comes next.
</p>

<p>
First things first
</p>

<p>
I already have a GitHub pages website setup. It's as simple as
creating a repository called <code>YOURGITHUBUSER.github.io</code> (you can see
mine <a href="https://github.com/clarete/clarete.github.io/">here</a>). After that, you can push static HTML content and it will
be available under <code>https://YOURGITHUBUSER.github.io</code>.
</p>

<p>
I also configured my DNS provider to point my domain <code>clarete.li</code> to
GitHub and created a <code>CNAME</code> file containing the domain name.
</p>

<p>
If you've never done that before, I highly recommend visiting the
GitHub Pages documentation. Specifically the articles for <a href="https://help.github.com/en/github/working-with-github-pages">creating a
new website</a> and <a href="https://help.github.com/en/github/working-with-github-pages/configuring-a-custom-domain-for-your-github-pages-site">configuring a custom domain</a> for your new website.
The home page
</p>

<p>
My lil website is as simple as it can get. My amazing <a href="https://www.instagram.com/pinkandsalt/">fiance</a> got me
the logo you see on the top bar and I took care of putting it on a
proper pink background for the internet enjoyment.
</p>

<p>
The index file is just HTML and CSS, nothing else. Even the list of
recent posts is done manually.
The Posts
</p>

<p>
This very post is written as an Org-Mode file. For those who haven't
heard of Org-Mode before, checkout <a href="https://orgmode.org/">their website</a>.  Long story short,
Org-Mode is a text-based document format.  From that perspective, it
could be compared with <code>Markdown</code>. The difference is the ginormous
feature set that Org-Mode supports compared.  Including, but not
limited to, many output formats and an incredible interactive
experience. 
</p>

<p>
HTML is one of the output formats that Org-Mode supports. Each post
written in an <code>.org</code> file will be translated into an HTML page.
This feature is built into Org-Mode. It just takes a bit of
configuration.
Org-Mode Publishing
</p>

<p>
Being completely honest, the HTML publishing feature doesn't seem
exactly designed for the blogging use case. It took quite a bit of
tweaking of the configuration to get it to do all the things that I
wanted.
</p>

<p>
You can read the entirety of my configuration in the file that I
submitted to git as part of this website: <a href="https://github.com/clarete/clarete.github.io/blob/master/publish">publish</a>. But there is one
little part that I consider worth mentioning: Setting the variable
<code>org-publish-project-alist</code>.
</p>

<p>
Although a single document can be exported to HTML, in order to
apply the same configuration to various files, I was required to
create a project with a source directory and an output directory.
</p>

<p>
The cool thing about this project thing was that I could have
different groups of settings and aggregate them all under a single
project.  The commented snippet bellow will shad some light in the
most essential parts of the configuration:
</p>

<div class="org-src-container">
<pre class="src src-emacs-lisp"><span style="color: #458588;">(</span><span style="color: #fb4933;">setq</span> org-publish-project-alist
  <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Here's the project definition with other 3 components</span>
  `<span style="color: #b16286;">(</span><span style="color: #8ec07c;">(</span><span style="color: #b8bb26;">"blog"</span> <span style="color: #fe8019;">:components</span> <span style="color: #d65d0e;">(</span><span style="color: #b8bb26;">"blog-posts"</span> <span style="color: #b8bb26;">"blog-static"</span> <span style="color: #b8bb26;">"blog-rss"</span><span style="color: #d65d0e;">)</span><span style="color: #8ec07c;">)</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">This is the component that translates the .org files into</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">the .html ones. There are lots of things happening here</span>
    <span style="color: #8ec07c;">(</span><span style="color: #b8bb26;">"blog-posts"</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Where your org files are coming from</span>
     <span style="color: #fe8019;">:base-directory</span> ,base-dir
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Where your HTML files will be generated</span>
     <span style="color: #fe8019;">:publishing-directory</span> ,pub-dir
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Customize the Org file tree before it's translated into</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">HTML. I needed to override this function to hook up</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">inserting the date of the post into the subtitle.</span>
     <span style="color: #fe8019;">:publishing-function</span> local-blog-publish
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Some configuration of what is going to be auto-generated</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">and what won't</span>
     <span style="color: #fe8019;">:auto-sitemap</span> t
     <span style="color: #fe8019;">:auto-preamble</span> t
     <span style="color: #fe8019;">:section-numbers</span> nil
     <span style="color: #fe8019;">:table-of-contents</span> nil
     <span style="color: #fe8019;">:html-head-include-default-style</span> nil
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">These allowed me to inject my own HTML for the header and</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">the footer. I'm in fact reading these two snippets from</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">separate files</span>
     <span style="color: #fe8019;">:html-preamble</span> local-blog-preamble
     <span style="color: #fe8019;">:html-postamble</span> local-blog-postamble<span style="color: #8ec07c;">)</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Tis is actually straight forward. It will take care of the</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">asset files included in the blog posts</span>
    <span style="color: #8ec07c;">(</span><span style="color: #b8bb26;">"blog-static"</span>
     <span style="color: #fe8019;">:base-directory</span> ,base-dir
     <span style="color: #fe8019;">:publishing-directory</span> ,pub-dir
     <span style="color: #fe8019;">:base-extension</span> <span style="color: #b8bb26;">"css</span><span style="color: #b8bb26; font-weight: bold;">\\</span><span style="color: #b8bb26; font-weight: bold;">|</span><span style="color: #b8bb26;">js</span><span style="color: #b8bb26; font-weight: bold;">\\</span><span style="color: #b8bb26; font-weight: bold;">|</span><span style="color: #b8bb26;">png</span><span style="color: #b8bb26; font-weight: bold;">\\</span><span style="color: #b8bb26; font-weight: bold;">|</span><span style="color: #b8bb26;">jpg</span><span style="color: #b8bb26; font-weight: bold;">\\</span><span style="color: #b8bb26; font-weight: bold;">|</span><span style="color: #b8bb26;">gif</span><span style="color: #b8bb26; font-weight: bold;">\\</span><span style="color: #b8bb26; font-weight: bold;">|</span><span style="color: #b8bb26;">pdf"</span>
     <span style="color: #fe8019;">:recursive</span> t
     <span style="color: #fe8019;">:publishing-function</span> org-publish-attachment<span style="color: #8ec07c;">)</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">Also straightforward, but needed to install an extension</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">`</span><span style="color: #d3869b;">ox-rss</span><span style="color: #7c6f64;">' that wasn't available via package manager. This little</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">snippet will be responsible for generating the `</span><span style="color: #d3869b;">rss.xml</span><span style="color: #7c6f64;">'</span>
    <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">file.</span>
    <span style="color: #8ec07c;">(</span><span style="color: #b8bb26;">"blog-rss"</span>
     <span style="color: #fe8019;">:base-directory</span> ,base-dir
     <span style="color: #fe8019;">:publishing-directory</span> ,pub-dir
     <span style="color: #fe8019;">:publishing-function</span> <span style="color: #d65d0e;">(</span>org-rss-publish-to-rss<span style="color: #d65d0e;">)</span>
     <span style="color: #fe8019;">:base-extension</span> <span style="color: #b8bb26;">"org"</span>
     <span style="color: #fe8019;">:rss-extension</span> <span style="color: #b8bb26;">"xml"</span>
     <span style="color: #fe8019;">:html-link-home</span> <span style="color: #b8bb26;">"https://clarete.li/"</span>
     <span style="color: #fe8019;">:html-link-use-abs-url</span> t
     <span style="color: #fe8019;">:include</span> <span style="color: #d65d0e;">(</span><span style="color: #b8bb26;">"rss.org"</span><span style="color: #d65d0e;">)</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">This is what generates the file `</span><span style="color: #d3869b;">rss.org</span><span style="color: #7c6f64;">', which in turn is</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">what's used to generate `</span><span style="color: #d3869b;">rss.xml</span><span style="color: #7c6f64;">'. The trickiest part for sure</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">was stitching together the custom functions starting with</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">`</span><span style="color: #d3869b;">local-blog-</span><span style="color: #7c6f64;">'. The references I linked in the last paragraph</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">can be of great help if my `</span><span style="color: #d3869b;">publish</span><span style="color: #7c6f64;">' file isn't enough for</span>
     <span style="color: #7c6f64;">;; </span><span style="color: #7c6f64;">you.</span>
     <span style="color: #fe8019;">:auto-sitemap</span> t
     <span style="color: #fe8019;">:sitemap-filename</span> <span style="color: #b8bb26;">"rss.org"</span>
     <span style="color: #fe8019;">:sitemap-title</span> <span style="color: #b8bb26;">"Lincoln Clarete"</span>
     <span style="color: #fe8019;">:sitemap-style</span> list
     <span style="color: #fe8019;">:sitemap-sort-files</span> anti-chronologically
     <span style="color: #fe8019;">:sitemap-format-entry</span> local-blog-sitemap-format-entry
     <span style="color: #fe8019;">:sitemap-function</span> local-blog-sitemap-function
     <span style="color: #fe8019;">:publishing-function</span> local-blog-rss-publish-to-rss<span style="color: #8ec07c;">)</span><span style="color: #b16286;">)</span><span style="color: #458588;">)</span>
</pre>
</div>
<p>
Operating the blog
</p>

<p>
From now one, I just have to execute the command <code>C-c C-e P p</code> to
ask Org Publishing to generate the HTML file of all the org files that
have been updated since I last executed it. It feels quite
convenient, I have to say.
</p>

<p>
After changing stuff and regenerating the HTML, the last step in my
publication journey is to add the changes to Git and push the
changes to GitHub. Then it just takes a minute or two for GitHub
Pages to pick up the changes and display it properly.
Open issues
</p>

<p>
I'm very happy with the current setup and there really aren't that
many issues so far. But there are two that I want to tackle:
</p>

<p>
I haven't added the unfurling links as suggested by <a href="https://writepermission.com/org-blogging-unfurling-links.html">rw-r-r</a>;
Still need to decide if I'll use Google Analytics or another less
intrusive alternative;
Source code snippets don't get syntax highlight if I the script
<code>publish</code> directly from the terminal. It errors out with the
following message: <code>Cannot fontify source block (htmlize.el &gt;=
       1.34 required)</code>. Which is quite weird because I have <code>htmlize
       1.57</code> installed.  I'm OK not fixing it for now because I can just
do it from within emacs and that's actually more convenient.
Final thoughts
</p>

<p>
The fine grained customizations were certaingly the hardest to get
to work and I wouldn't have been able to figure it out in the amount
of time I had to dedicate to this task without the amazing reference
other Emacs users put together. Here's what I consulted in no
particular order:
</p>

<p>
<a href="https://orgmode.org/worg/org-tutorials/org-publish-html-tutorial.html">https://orgmode.org/worg/org-tutorials/org-publish-html-tutorial.html</a>
<a href="https://www.brautaset.org/articles/2017/blogging-with-org-mode.html">https://www.brautaset.org/articles/2017/blogging-with-org-mode.html</a>
<a href="https://vicarie.in/posts/blogging-with-org.html">https://vicarie.in/posts/blogging-with-org.html</a>
<a href="https://writepermission.com/org-blogging-rss-feed.html">https://writepermission.com/org-blogging-rss-feed.html</a>
</p>
]]></description>
</item>
</channel>
</rss>
